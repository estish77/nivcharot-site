# dependencies
node_modules/

# next.js
.next/
out/

# production
build/
dist/

# payload
payload-types.ts
# Local SQLite DB backing the admin dashboard (2026-08-16) — real editorial
# content, never belongs in git; back it up separately if it matters.
payload.db
payload.db-*
# Local fallback for uploaded photos (see Media.ts) — same reasoning as
# payload.db above: real content, not source, and in production it lives in
# Vercel Blob instead (see payload.config.ts), not this folder.
/media/

# env
.env
.env.local
.env.*.local
.env.development
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea
