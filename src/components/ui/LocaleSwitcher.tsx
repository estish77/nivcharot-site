/**
 * @deprecated Renamed to `LanguageToggle` to match the shared-components
 * spec's naming. Kept as a re-export so any existing import of
 * `LocaleSwitcher` keeps working — prefer importing `LanguageToggle`
 * directly in new code.
 */
export { LanguageToggle as LocaleSwitcher, type LanguageToggleProps as LocaleSwitcherProps } from './LanguageToggle'
